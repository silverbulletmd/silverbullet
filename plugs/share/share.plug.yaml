name: share
functions:
  publishCommand:
    path: publish.ts:publishCommand
    command:
      name: "Share: Publish"
      key: "Ctrl-s"
      mac: "Cmd-s"
  publish:
    path: publish.ts:publish
    env: server
