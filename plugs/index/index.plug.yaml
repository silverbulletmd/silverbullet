name: index
functions:
  # Public API
  extractFrontmatter:
    path: api.ts:extractFrontmatter
    syscall: index.extractFrontmatter
  patchFrontmatter:
    path: api.ts:patchFrontmatter
    syscall: index.patchFrontmatter
  text:
    path: indexer.ts:indexMarkdown
    syscall: index.markdown
  resolveAnchor:
    path: api.ts:resolveAnchor
    syscall: index.resolveAnchor
  queryLuaObjects:
    # DEPRECATED: use index.queryLuaObjects syscall instead
    path: api.ts:queryLuaObjects
  getObjectByRef:
    # DEPRECATED: use index.getObjectByRef syscall instead
    path: api.ts:getObjectByRef

  # YAML syscalls
  yamlParse:
    path: yaml.ts:parse
    syscall: yaml.parse

  yamlStringify:
    path: yaml.ts:stringify
    syscall: yaml.stringify

  reindexSpaceCommand:
    path: "./command.ts:reindexCommand"
    command:
      name: "Space: Reindex"
      menu: { location: space, group: "2_index", order: 1, label: "Reindex Space" }

  processIndexQueue:
    path: ./queue.ts:processIndexQueue
    mqSubscriptions:
      - queue: indexQueue
        batchSize: 15
        autoAck: true

  processIndexQueuePass1:
    path: ./queue.ts:processIndexQueuePass1
    mqSubscriptions:
      - queue: indexQueuePass1
        batchSize: 15
        autoAck: true

  indexDocument:
    path: "./document.ts:indexDocument"
    events:
      - document:index

  indexPage:
    path: indexer.ts:indexPage
    events:
      - page:index

  indexPagePass1:
    path: indexer.ts:indexPagePass1
    events:
      - page:indexPass1

  # Headers
  headerComplete:
    path: header.ts:headerComplete
    events:
      - editor:complete

  # Anchors
  anchorComplete:
    path: complete.ts:anchorComplete
    events:
      - editor:complete

  # Hashtags
  tagComplete:
    path: tags.ts:tagComplete
    events:
      - editor:complete

  frontmatterTagComplete:
    path: tags.ts:frontmatterTagComplete
    events:
      - editor:complete

  frontmatterComplete:
    path: frontmatter.ts:frontmatterComplete
    events:
      - editor:complete

  renamePageCommand:
    path: "./refactor.ts:renamePageCommand"
    command:
      name: "Page: Rename"
      menu: { location: file, group: "4_page", order: 1, label: "Rename Page..." }
      page: ""
      requireMode: rw
      requireEditor: page
  renamePrefixCommand:
    path: "./refactor.ts:renamePrefixCommand"
    command:
      name: "Page: Batch Rename Prefix"
      requireMode: rw
      requireEditor: page
  renamePageLinkCommand:
    path: "./refactor.ts:renamePageLinkCommand"
    command:
      name: "Page: Rename Linked Page"
      requireMode: rw
      requireEditor: page

  # Refactoring Commands

  renameDocumentCommand:
    path: "./refactor.ts:renameDocumentCommand"
    command:
      name: "Document: Rename Document"
      requireMode: rw
      requireEditor: notpage

  extractToPageCommand:
    path: ./refactor.ts:extractToPageCommand
    command:
      name: "Page: Extract"
      requireMode: rw
      requireEditor: page

  refreshWidgets:
    path: widget.ts:refreshWidgets

  lintYAML:
    path: lint.ts:lintYAML
    events:
      - editor:lint

  lintLua:
    path: lint.ts:lintLua
    events:
      - editor:lint

  lintObjects:
    path: lint.ts:lintObjects
    events:
      - editor:lint

  lintAnchors:
    path: lint.ts:lintAnchors
    events:
      - editor:lint

  # X-Ray editor lens
  xrayInfo:
    path: xray.ts:xrayInfo
    events:
      - editor:lint

  toggleXRayMode:
    path: xray.ts:toggleXRayMode
    command:
      name: "Editor: Toggle X-Ray"
      requireEditor: page

  # Tasks
  updateTaskState:
    path: task.ts:updateTaskState

  taskToggle:
    path: "./task.ts:taskToggle"
    events:
      - page:click
  cycleTaskStateByRef:
    path: ./task.ts:cycleTaskStateByRef
  taskToggleCommand:
    path: ./task.ts:taskCycleCommand
    command:
      name: "Task: Cycle State"
      key: Mod-. t
      requireMode: rw
      requireEditor: page

  taskComplete:
    path: ./complete.ts:completeTaskState
    events:
      - editor:complete

  removeCompletedTasksCommand:
    path: task.ts:removeCompletedTasksCommand
    command:
      name: "Task: Remove Completed"
      requireMode: rw
      requireEditor: page
