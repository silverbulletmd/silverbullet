# To build:
# docker buildx build --platform linux/amd64,linux/arm64 -t zefhemel/silverbullet-website:v1 -f Dockerfile.website --push .
FROM zefhemel/silverbullet:edge

ENV SB_READ_ONLY=1
ENV SB_INDEX_PAGE=SilverBullet

ADD ./website /space
